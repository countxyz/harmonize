<section id="notifications">
  <p>Last accessed: <%= Time.now.to_s(:long_ordinal) %></p>
  <% if notice %>
  <p class="notification notice">
    <%= notice %>
  </p>
  <% end %>

  <% if alert %>
  <p class="notification alert">
    <%= alert %>
  </p>
  <% end %>
</sections>